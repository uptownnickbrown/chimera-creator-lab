# Pregenerated art lives here and is COMMITTED (Agora convention).
#
# <Asset slot="x/y"> loads /assets/x/y.webp and falls back to a labelled plate
# until the file exists. Slots the UI asks for today:
#   ui/logo, ui/avatar, ui/tbd
#   icons/nav_{home,fusion,codex,arena}
#   icons/stat_{power,speed,armor,size,special}
#   icons/quick_{total,wins,biggest,champion}
#   icons/tile_{create,my,battle,hall}
#   icons/sort_{newest,favorites,winners,biggest,fastest,strongest}
#   icons/{xp,creatures,ability,fact_strength,fact_fun}
#   icons/reason_{armor,speed,power,size,special,environment,endurance,range}
#   icons/record_{biggest,fastest,strongest,most_wins}
#   sources/<slug>, environments/<slug>, creatures/<id>
